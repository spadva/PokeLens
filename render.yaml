services:
  - type: web
    name: pokelens
    runtime: python
    buildCommand: echo "No build needed"
    startCommand: python server.py
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
